// Generated by CoffeeScript 1.4.0
var Colors, esc, eventColors, methodColors, statusColors;

esc = '\x1B[';

Colors = {
  black: esc + '30m',
  bg_black: esc + '40m',
  red: esc + '31m',
  bg_red: esc + '41m',
  green: esc + '32m',
  bg_green: esc + '42m',
  yellow: esc + '33m',
  bg_yellow: esc + '43m',
  blue: esc + '34m',
  bg_blue: esc + '44m',
  magenta: esc + '35m',
  bg_magenta: esc + '45m',
  cyan: esc + '36m',
  bg_cyan: esc + '46m',
  white: esc + '37m',
  bg_white: esc + '47m',
  reset: esc + '0m',
  bold: esc + '1m',
  underline: esc + '4m',
  status: function(status) {
    var c, k;
    for (k in statusColors) {
      c = statusColors[k];
      if (status >= k) {
        break;
      }
    }
    return c + status + Colors.reset;
  },
  method: function(method) {
    var _ref;
    return ((_ref = methodColors[method]) != null ? _ref : Colors.yellow) + method + Colors.reset;
  },
  event: function(event) {
    var _ref;
    return ((_ref = eventColors[event]) != null ? _ref : Colors.green) + event + Colors.reset;
  }
};

module.exports = Colors;

eventColors = {
  'error': Colors.red
};

methodColors = {
  GET: Colors.green,
  POST: Colors.cyan,
  PUT: Colors.cyan,
  DELETE: Colors.red
};

statusColors = {
  500: Colors.red,
  400: Colors.yellow,
  300: Colors.cyan,
  _: Colors.green
};
